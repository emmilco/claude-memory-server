### Fixed
- **BUG-368: Missing Range Validators for Timeout and Pool Configuration**
  - Added field validators for `qdrant_pool_timeout`, `qdrant_pool_recycle`, and `qdrant_health_check_interval` to ensure all values are > 0
  - Prevents invalid negative or zero timeout/interval configurations that could cause runtime issues
  - Files: src/config.py
