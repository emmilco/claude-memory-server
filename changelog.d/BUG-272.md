### Fixed
- **BUG-272: Fix Memory Leak in ProcessPoolExecutor Cleanup**
  - Fixed memory leak caused by circular references in atexit handler registration
  - Replaced bound method registration with module-level cleanup handlers
  - Introduced module-level weak set for instance tracking to prevent garbage collection issues
  - Module-level handlers registered once at import time instead of per-instance
  - Signal handlers (SIGTERM/SIGINT) now use module-level function iterating weak set
  - Ensures proper garbage collection of ParallelEmbeddingGenerator instances
  - Files: src/embeddings/parallel_generator.py
